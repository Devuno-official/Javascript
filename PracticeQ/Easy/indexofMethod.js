// 5) Write a program that finds the index of a given element in an array, starting the search from a specified index.

 let arr = ["a", "b", "c", "d", "e"];
  console.log(arr.indexOf("b",2));
                    // Output: -1 (since "b" is not found starting from index 2)